import {expect} from 'chai';
import {stub} from 'sinon';
import Chance from 'chance';

import GridStrategy from '../../src/view-strategies/grid';

describe('Grid strategy tests', () => {
  let chance,
    groups;

  beforeEach('set up', () => {
    chance = new Chance();

    groups = [

    ];
  });

  describe('when calling next()', () => {
  });
});
